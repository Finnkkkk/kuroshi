# Porta em que o backend Express vai escutar
PORT=4000

# Qual API de anime usar como fonte de dados: "jikan" (padrão, MyAnimeList)
# ou "anilist" (GraphQL, dados mais completos como banner e sinopse maior)
PROVIDER=jikan
